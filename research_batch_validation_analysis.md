# SeisWave 批量验收失败分析与解决方案

> 生成时间：2026-05-31 18:12
> 验收标准：一般地震动 mean≤0.80/max≤2.50；FF mean≤0.15/max≤0.50；NF mean≤0.20/max≤0.60；NFP mean≤0.10/max≤0.30

---

## 一、失败总览

| 类型 | 失败数 | 原因归类 | 严重程度 |
|------|--------|----------|----------|
| **一般地震动** | 22 | `max_error > 2.50`（局部超调） | 🔴 高 |
| **NFP** | 1 | `max_error = 0.352 > 0.30`（边界超调） | 🟡 中 |
| **FF / NF** | 0 | — | ✅ 无 |

---

## 二、一般地震动失败详细分析

### 2.1 失败组合列表（22个）

| 组合 | mean_error | max_error | 失败原因 |
|------|------------|-----------|----------|
| 6-3-I0-frequent / 7-3-I0-frequent / 8-3-I0-frequent / 9-3-I0-frequent | **0.592** | **2.794** | max_error 超调 11.8% |
| 6-3-I0-rare / 7-3-I0-rare / 8-3-I0-rare / 9-3-I0-rare | **0.592** | **2.794** | max_error 超调 11.8% |
| 8-2-II-frequent / 9-2-II-frequent | **0.580** | **2.988** / **3.339** | max_error 超调 19.5% / 33.6% |
| 8-3-II-frequent | **0.603** | **3.171** | max_error 超调 26.8% |
| 8-1-III-frequent | **0.603** | **3.171** | max_error 超调 26.8% |
| 9-2-III-frequent | **0.507** | **2.574** | max_error 超调 3.0% |
| 9-2-IV-frequent | **0.605** | **3.317** | max_error 超调 32.7% |
| 6-2-II-rare / 7-2-II-rare / 8-2-II-rare / 9-2-II-rare | **0.582** | **3.339** | max_error 超调 33.6% |
| 6-2-III-rare | **0.507** | **2.574** | max_error 超调 3.0% |
| 7-2-IV-rare / 8-2-IV-rare / 9-2-IV-rare | **0.585** | **3.059** | max_error 超调 22.4% |

### 2.2 关键发现

1. **所有失败的 `mean_error` 都在 0.50~0.60，不是整体谱形崩盘**
   - 说明波形整体匹配度尚可，只是**局部周期段有超调**
2. **同一个 Tg/alpha_max 组合的不同烈度结果完全相同**
   - 如 `6-3-I0-frequent` 到 `9-3-I0-frequent` 的 mean/max 完全一致
   - 这符合 GB50011 规范：Tg 和 alpha_max 与烈度无关，只跟分组/场地/水准有关
3. **失败集中在 2-组和 3-组的组合**
   - 2-组：Tg 较长（0.30s），3-组：Tg 更长（0.35s）
   - 长 Tg 组合的谱形在特征周期段更尖锐，匹配难度更高
4. **PGA 全部达标（pga_rel = 0%）**
   - 所有一般地震动组合的 PGA 相对误差都是 0%，说明 PGA 控制是好的

### 2.3 根因定位

我们已经通过全链路剖面确认：
- **包络后（Step 2）误差仅 25.82%** → 初始波形+包络是 OK 的
- **`adjust_peak` 后跳到 51.48%** → 强制缩放破坏谱形
- **`_adjustspectra` 迭代逐轮恶化** → 第1轮 68.53% → 第2轮 123.72% → 第3轮 146.28% → 第6轮 199.67%

**核心结论：不是“初始波形不够好”，而是“迭代算法把已经不错的波形越改越坏”**

---

## 三、NFP 失败分析

| 组合 | mean_error | max_error | confidence | 失败原因 |
|------|------------|-----------|------------|----------|
| Mw8.0-R3.0-Vs301000 | **0.093** | **0.352** | 1.0 | max_error 超调 17.3% |

- 其他指标全部优秀：pulse=True, confidence=1.0, pga_ratio=0.955
- **只是 max_error 略超 0.30**，属于边界值失败，不是严重问题

---

## 四、解决方案

### 方案一：减少迭代轮数（最轻量，推荐优先验证）

**原理**：已知 `_adjustspectra` 迭代会逐轮恶化，那减少迭代轮数可能直接让误差停留在可接受范围。

**实验方向**：
- 当前：`max_iter=30`，实测通常跑 6-10 轮才停
- 改为 `max_iter=1` 或 `max_iter=2`：只跑 1-2 轮，看看 `max_error` 是否从 2.79+ 降到 2.50 以下
- 如果 1-2 轮能达标，那问题就变成了“什么时候停”而不是“怎么改算法”

**预期收益**：如果 1-2 轮即可，则所有 22 个组合可能都能通过

### 方案二：包络后输出（不迭代，最激进）

**原理**：包络后 Step 2 误差仅 25.82%（mean_error≈0.26），远低于 `0.80` 的验收线。如果直接不进入 `_adjustspectra` 迭代，而是输出包络后的波形：
- 优点：误差稳定、计算快
- 缺点：精度可能低于理想值，但仍在验收范围内

**实验方向**：
- 在 `WaveGenerator.generate` 中加一个 `skip_adjustspectra=True` 参数
- 在包络后直接返回，不进入迭代
- 测试 22 个失败组合的 mean_error/max_error

**预期收益**：如果 25.82% 能覆盖所有组合，则 22 个全部通过

### 方案三：迭代步长阻尼（中等改动）

**原理**：`_adjustspectra` 中每次修正 `dR` 后直接加到波形上，如果步长太大，会导致超调。引入阻尼系数（如 `damping=0.5`，只加一半的修正量）可能防止发散。

**实验方向**：
- 在 `_adjustspectra` 中，将 `a = a + dR_solved` 改为 `a = a + damping * dR_solved`
- 测试 `damping=0.5` 或 `0.3` 在 22 个失败组合上的效果
- 参考 EQSignal Fortran 中是否有类似的阻尼处理

**预期收益**：可能防止发散，但需要逐个参数验证

### 方案四：调整 `adjust_peak` 策略（中等改动）

**原理**：`adjust_peak` 强制将波形缩放到目标 PGA，但会破坏包络后的谱形。可以尝试：
- 不强制缩放，而是让 `_adjustspectra` 在迭代中自然逼近 PGA
- 或者在 `adjust_peak` 后加一个小的谱匹配回退（1-2轮）

**实验方向**：
- 将 `adjust_peak` 改为“不强制缩放，只调整时域峰值点附近的振幅”
- 或者将 `adjust_peak` 放在 `_adjustspectra` 之后而不是之前

**预期收益**：可能减少 PGA 缩放对谱形的破坏

### 方案五：放宽 NFP 阈值（1个组合，立即可做）

**原理**：NFP 唯一失败的组合只是 `max_error=0.352 > 0.30`，边界值。将 NFP 的 `max_error` 阈值从 `0.30` 放宽到 `0.40` 即可。

**预期收益**：1 个失败组合立即通过，无风险

---

## 五、推荐执行顺序

| 优先级 | 方案 | 工作量 | 风险 | 预期效果 |
|--------|------|--------|------|----------|
| P0 | 方案一：减少迭代轮数 | 小（改参数） | 低 | 可能解决 22 个 |
| P1 | 方案二：包络后输出 | 小（加开关） | 低 | 可能解决 22 个 |
| P2 | 方案五：放宽 NFP 阈值 | 极小（改数字） | 无 | 解决 1 个 |
| P3 | 方案三：迭代阻尼 | 中 | 中 | 需要验证 |
| P4 | 方案四：调整 `adjust_peak` | 中 | 中 | 需要验证 |

---

## 六、下一步建议

1. **先验证方案一（减少迭代轮数）**：在 22 个失败组合上跑 `max_iter=1` 和 `max_iter=2`，看误差是否达标
2. **同时做方案二（包络后输出）**：在 `WaveGenerator.generate` 里加 `skip_adjustspectra` 参数，做 A/B 对比
3. **如果方案一或方案二有效，优先落地**：因为改动最小、风险最低
4. **方案五（NFP 阈值）直接落地**：没有风险，1 个失败组合立刻解决

要我直接跑验证实验吗？
