# SeisWave v3.0 需求规格

## 1. 项目定位

SeisWave 是一个面向中国结构工程师的地震波选取与生成桌面工具，核心目标：

> 按照 GB 50011-2010《建筑抗震设计规范》和 GB/T 51408-2021《建筑隔震设计标准》的地震动输入要求，从 PEER NGA 天然地震动数据库中自动选取天然波，并生成人工波，最终组合输出满足规范要求的 7 条（或 3 条）地震动时程。

技术基础：
- 人工波生成：复用 EQSignal (Fortran) 的谱匹配算法
- 天然波选取：参考 MATLAB `SelectWave_0802g.m` 的选波流程
- 信号处理：复用 EQSignal 的基线校正、滤波等功能

## 2. 用户角色

| 角色 | 描述 |
|------|------|
| 结构工程师 | 主要用户，需要为时程分析准备地震动输入 |

## 3. 核心功能需求

### US-1: 规范谱参数设置

用户输入结构和场地参数，软件自动生成目标反应谱。

**验收标准：**
- AC-1.1: 支持 GB 50011 抗震谱（设防烈度 6~9 度、分组 1~3、场地 I0~IV、多遇/基本/罕遇）
- AC-1.2: 支持隔震谱（GB/T 51408，三段式）
- AC-1.3: 隔震设计时支持输入隔震前周期和隔震后周期，分别匹配
- AC-1.4: 支持自定义阻尼比（默认 0.05）
- AC-1.5: 支持自定义规范谱（用户手动输入周期-谱值数据点，或导入 CSV/TXT）
- AC-1.6: 实时预览反应谱曲线
- AC-1.7: 支持输入结构主要周期 T1、T2、T3（用于选波匹配）

### US-2: 天然波自动选取

从内置 PEER NGA 数据库（236 条记录、701 个分量）中，按中国规范要求自动筛选最优天然波。

**PEER AT2 文件可解析的元数据（已确认）：**
- 文件名 → RSN 编号（如 RSN1004）
- 第 2 行 → 事件名（如 Northridge-01）、日期、台站名、分量方向角
- 第 4 行 → NPTS（数据点数）、DT（时间步长）
- 缺失：震级 Mw、断层距 Rrup、Vs30（需从 PEER 网站 SearchResults CSV 补充，列为后续增强）

**验收标准：**
- AC-2.1: 内置 PEER NGA 天然波数据库（701 条 AT2，随软件分发）
- AC-2.2: 解析并展示每条波的元数据：RSN 编号、事件名、台站名、日期、分量方向、NPTS、DT
- AC-2.3: 仅选取水平分量（自动排除竖向 UP/DWN 分量），约 468 条候选
- AC-2.4: 按中国规范（GB 50011）三步筛选流程：
  - 第一步：有效持时筛选（≥ 5T1，基于 5%-95% Arias 强度）
  - 第二步：反应谱偏差筛选（各主周期点偏差 ≤ 20%，或用户自定义容限）
  - 第三步（可选）：底部剪力校核（单条 0.65~1.35，平均 ≥ 0.80）
- AC-2.5: 隔震设计时，需同时满足隔震前周期和隔震后周期的反应谱匹配
- AC-2.6: 按反应谱匹配度排序，自动推荐最优的 N 条天然波
- AC-2.7: 支持 PGA 缩放（将天然波缩放到目标 PGA）
- AC-2.8: 选波结果可视化：每条波的反应谱 vs 目标谱对比图
- AC-2.9: 支持按 RSN 编号、事件名筛选/过滤
- AC-2.10（后续增强）: 支持导入 PEER SearchResults CSV 补充 Mw/Rrup/Vs30 元数据，支持按这些参数筛选

### US-3: 人工波生成

基于目标反应谱，迭代生成与规范谱匹配的人工地震波。

**验收标准：**
- AC-3.1: 支持时域法（adjustspectra）和频域法（fitspectra）两种谱匹配方法
- AC-3.2: 调用 EQSignal Fortran 编译库进行核心计算（f2py），纯 Python 作为兜底
- AC-3.3: 迭代过程实时显示进度（当前迭代次数、最大偏差、均值偏差）
- AC-3.4: 收敛后输出人工波时程及反应谱对比图
- AC-3.5: 支持多 trial 随机种子，自动取最优结果
- AC-3.6: 50 次迭代应在 10 秒内完成（Fortran 加速后）

### US-4: 地震动组合输出

按规范要求的天然波/人工波比例，组合输出最终地震动集。

**验收标准：**
- AC-4.1: 7 条模式：不少于 5 条天然波 + 不少于 2 条人工波（GB 50011 第 5.1.2 条）
- AC-4.2: 3 条模式：不少于 2 条天然波 + 不少于 1 条人工波
- AC-4.3: 7 条时取平均值作为计算结果，3 条时取包络值
- AC-4.4: 组合后校验：
  - 每条波底部剪力 ≥ CQC 法的 65%，≤ 135%
  - 多条波平均底部剪力 ≥ CQC 法的 80%
- AC-4.5: 一键导出所有波形文件（AT2 格式 + TXT 格式）
- AC-4.6: 导出选波报告（包含参数、筛选过程、反应谱对比图）

### US-5: 信号处理工具

对单条地震波进行处理和分析。

**验收标准：**
- AC-5.1: 基线校正（多项式去趋势、双线性去趋势）
- AC-5.2: Butterworth 滤波（带通/低通/高通）
- AC-5.3: 加速度 → 速度 → 位移积分
- AC-5.4: 反应谱计算（Newmark-β / 频域 / 混合法）
- AC-5.5: 傅里叶谱、功率谱密度
- AC-5.6: Arias 强度、有效持时
- AC-5.7: 重采样、裁剪、归一化、缩放
- AC-5.8: 处理前后对比可视化

## 4. 非功能需求

### NFR-1: 性能
- 701 条天然波批量加载 ≤ 10 秒
- 701 条波的反应谱批量计算 ≤ 60 秒（Fortran 加速）
- 单条人工波生成（50 次迭代）≤ 10 秒（Fortran 加速）
- GUI 在计算过程中保持响应（不冻结）

### NFR-2: 可用性
- macOS 原生支持（用户主力平台）
- 深色/浅色主题切换
- 中文界面

### NFR-3: 可维护性
- 核心算法与 GUI 完全解耦
- Fortran 加速层可选（纯 Python 兜底）
- 单元测试覆盖核心算法

## 5. 数据源

| 数据 | 路径 | 说明 |
|------|------|------|
| PEER NGA 天然波 | `data/peer_nga/` | 236 条记录 × 3 分量 = 701 个 AT2 文件，86MB |
| EQSignal Fortran | `/Users/yachiyo/Developer/EQSignal_ref/` | 人工波生成核心算法源码 |
| MATLAB 选波参考 | `matlab_ref/` | 原始 MATLAB 选波程序 |

## 6. 现有代码资产（可复用）

| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 文件 I/O | `core/io.py` | ✅ 可用 | AT2/TXT/CSV 读写，批量加载 |
| 规范谱 | `core/code_spec.py` | ✅ 可用 | GB50011/EC8/ASCE7 谱计算 |
| 信号处理 | `core/signal.py` | ✅ 可用 | EQSignal 类，基线校正/滤波/积分 |
| 反应谱 | `core/spectrum.py` | ✅ 可用 | Newmark-β/频域/混合法 |
| 选波引擎 | `core/selector.py` | ⚠️ 需改造 | 有框架但未接入 PEER 数据库 |
| 人工波生成 | `core/generator.py` | ⚠️ 需改造 | 纯 Python 太慢，需集成 Fortran |
| GUI 主窗口 | `gui/main_window.py` | ⚠️ 需重构 | 面板结构需调整，增加组合输出流程 |
| GUI 面板 | `gui/panels/*.py` | ⚠️ 需重构 | 选波面板需重做，增加组合面板 |

## 7. 技术约束

- Python 3.11+, PySide6 (Qt6)
- NumPy, SciPy, Matplotlib
- Fortran 编译：numpy.f2py + gfortran
- 打包：PyInstaller（macOS .app）

## 8. 工作流程（用户视角）

```
┌─────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  1. 设置     │───▶│  2. 选天然波  │───▶│  3. 生成人工波│───▶│  4. 组合输出  │
│  规范谱参数  │    │  自动筛选5条  │    │  生成2条     │    │  7条/3条     │
│  T1/T2/T3   │    │  (从PEER库)  │    │  (Fortran)   │    │  导出+报告   │
└─────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
```

## 9. 优先级

| 优先级 | 功能 | 理由 |
|--------|------|------|
| P0 | Fortran 编译集成 | 性能瓶颈，阻塞人工波生成 |
| P0 | 天然波自动选取 | 核心功能，当前缺失 |
| P0 | 组合输出（5+2 / 2+1） | 最终交付物 |
| P1 | GUI 重构 | 流程需要重新设计 |
| P1 | 选波报告导出 | 工程交付需要 |
| P2 | 信号处理增强 | 已有基础，锦上添花 |
